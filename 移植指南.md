# KiriKiri2 文字冒险 → 小米手环 移植指南

> 本指南以《DRACU-RIOT!》移植为完整示例，讲解如何把任何 **KiriKiri2 引擎** 的文字冒险游戏移植到小米手环快应用。
> 覆盖：解包 → 剧本反编译 → 结构分析 → 转换 → 分支配置 → 资源处理 → 播放器 → 验证。
> 每步都给**通用原理** + **本项目的具体实现**，下一个人可照着套用。

---

## 0. 目标平台与核心模型

| 项 | 说明 |
|---|---|
| 屏幕 | 336×480 竖屏（小米手环快应用） |
| 能力 | 图片、文字、震动 API；**无音频/视频** |
| 核心模型 | **全局线性页表**：page N → 一条文本/一个选项页；翻页 = `+1`；跳转由 `branchConfig` 特判 |
| 数据 | `scriptDataN.txt`（500 页/块）JSON，快应用 `file.readText` 分块加载 |

**关键思想**：把原作的分支剧情"压扁"成一条长线性流，再用 `branchConfig` 记录所有"偏离线性"的地方（跳转/堵死/路由/结局）。

---

## 1. 解包游戏（xp3）

- **KiriKiri2 双端版**：`*.xp3` 是标准格式（索引 zlib 压缩），可自写脚本或 GARbro 解包。
- **KiriKiriZ / QHD / Steam 版**：索引加密（Hxv4），需 `KrkrExtractForCxdecV2` + hash 还原。
- 解包后主要目录：
  - `scenario/` 或 `scn/`：`.ks.scn`（编译后剧本）
  - `fgimage/`：立绘（`.tlg` + 分层规则 `.txt`）
  - `bgimage/`、`evimage/`：背景、事件 CG
  - `image/`：特效图、SD、cutin 等
  - `sound/` `voice/` `video/`：音频视频（手环不用）

> 注意区分版本：**双端版（KiriKiri2）** 才能用本指南的管线；QHD 版解密步骤不同。

---

## 2. 剧本反编译（FreeMote）

用 **FreeMote `PsbDecompile.exe`** 批量把 `.ks.scn` → `.json`：

```
PsbDecompile.exe input.scn → input.json (+ input.resx.json)
```

输出 JSON 包含完整的场景结构（下面第 3 节），这是所有后续工作的基础。

---

## 3. FreeMote JSON 结构（必须吃透）

一个剧本文件（对应原作一个 `.ks`）的结构：

```jsonc
{
  "scenes": [
    {
      "label": "*Main_part_1",          // 场景标签（跳转目标）
      "lines": [...],                    // 场景指令（chapter 标题、对象操作）
      "texts": [...],                    // 对话块
      "selects": [...],                  // 选项列表（有才是选项场景）
      "nexts": [...]                     // 场景跳转（见 4.3）
    }
  ]
}
```

### 3.1 对话块 `texts[]`

```jsonc
[ "梓", null, "「你要去哪儿？」", [{"name":"梓","voice":"azu..."}], 16, {"objectList":[...]} ]
//  ^说话人 ^语音   ^文本                 ^语音          ^字号   ^当前页资源对象
```

- `texts[0]`：说话人
- `texts[2]`：文本（`\n` 换行；可能有 `%NN` 字号标记、`[xxx]` 注释要清理）
- `texts[5].objectList`：**该页的背景/立绘/CG/SD 对象**（重点）

### 3.2 资源对象 `objectList[]`

```jsonc
{ "cname": "stage",      "imageFile": {"file":"base.stage","options":{"stagename":"カフェ","timename":"夜"}} }
{ "cname": "character",  "imageFile": {"file":"梓.stand","options":{"dress":"私服","pose":"1","face":"07h"}}, "posName":"左" }
{ "cname": "event",      "imageFile": {"file":"ev606aa"} }
{ "cname": "sdlayer",    "imageFile": {"file":"sd104a"} }
```

| cname | 含义 | 手环字段 |
|---|---|---|
| `stage` | 背景（`stagename_timename` 组合） | `b` |
| `character` | 立绘（`dress/pose/face` + 位置） | `c` |
| `event` / `centerlayer` | 事件 CG | `cg` |
| `sdlayer` / `centerlayer`(SD) | SD 小人 | `sd` |

> `base.stage` 是背景索引，真正图片名 = `stagename_timename`（需建映射表）。

### 3.3 选项 `selects[]`

**两种模式（这是移植的核心难点，务必分清）**：

**模式 A：直接跳转**
```jsonc
{ "text": "和美羽一起", "target": "*part003_01A", "exp": "f.sel_flag = 1 , f.miu_flag++" }
```
→ 选项页的 c1t 直接指向分支场景。

**模式 B：flag + dummy 路由（H 场景大量使用）**
```jsonc
// 选项只有 exp，没有 target：
{ "text": "中出", "exp": "f.sel_flag = 0", "eval": "checkIN" }
{ "text": "外射", "exp": "f.sel_flag = 1", "eval": "checkOUT" }
// 选项页的 nexts → 指向一个 dummy 场景，dummy 的 nexts 按 sel_flag 路由：
"nexts": [{ "eval": "f.sel_flag == 1", "target": "*eri04_01B" }, { "target": "*eri04_01A" }]
```
→ 玩家选"外射"→ sel_flag=1 → dummy 场景路由到 `eri04_01B`；否则默认 `eri04_01A`。

> `eval` 在选项上 = **可见条件**（如 checkIN/checkOUT，可简化全部显示）；在 nexts 上 = **跳转条件**。

### 3.4 场景跳转 `nexts[]`（语义最重要）

```jsonc
"nexts": [
  { "eval": "f.rio_flag == 4", "target": "*route_rio" },   // 条件分支（顺序匹配）
  { "eval": "f.eri_flag == 4", "target": "*route_eri" },
  { "target": "*normal_end" }                                // ★ 最后一个无 eval 的 = 默认分支
]
```

**规则：数组里"带 eval 的条件分支"在前，最后一个无 eval 的是默认分支，顺序匹配，全部不命中走默认。**

---

## 4. 分支系统解析（移植前必读）

### 4.1 flag 变量
原作用 flag 记录剧情状态，三类：
- `f.sel_flag`：H 场景选项临时状态（可被多次覆盖）
- `f.xxx_flag`（女孩好感）：选项 `++` 累计，控制路线判定（如 `f.miu_flag == 4 → 美羽线`）
- `sf.xxx`：全局解锁（回想/二周目，移植可忽略）

### 4.2 选项设置 flag
选项的 `exp` 字段设置 flag：`f.miu_flag ++`、`f.sel_flag = 2` 等。
**每个选项页要解析出"选第 N 项 → flag 变化"的规则表**（参考 `flag_rules.md`）。

### 4.3 路线判定（flag 累计 → 分叉）
常见结构：共通线各选项给不同女主 flag `++`，结局前一个 dummy 场景按 `f.xxx_flag == 4` 路由到各路线。
→ 这是移植里最复杂的 hiddenPages（见第 7 节）。

### 4.4 其他坑
| 现象 | 处理 |
|---|---|
| `*label:2` 跳行 | FreeMote 保留为**独立标签**，按完整标签解析（先全名后剥 `:N`） |
| `kag.isRecollection` 条件 | 回想模式专用，正常游玩不触发，**忽略** |
| `sf.clear_*` | 二周目路线解锁，移植**简化忽略** |
| `hselectFix/OUTIN` | H 选项可见性锁定，可**全部显示**简化 |
| 向后跳转 | 可能是合法回路（如分支演完回判定点），校验时别当 bug |

---

## 5. 剧本转换（JSON → 线性页表）

每场景按顺序产出连续页：
```
场景 = [chapter标题行?] + [N 条对话] + [选项页?]
```

页条目格式（本项目）：

```jsonc
{ "s":"梓", "t":"「你要去哪儿？」", "b":"カフェ_夜", "c":"梓?dress=私服&pose=1&face=07h@左", "cg":"", "sd":"", "cs":100, "blur":false, "e":false, "f":false }
{ "co":true, "c1":"中出", "c1t":"*eri04_01B", "c2":"外射", "c2t":"*eri04_01A", "b":..., "c":... }
```

要点：
- **文本清理**：去 `[xxx]`、`%0`、控制字符；`%NN` 提取为字号 `fs`
- **说话人**：`texts[0]`（空 = 旁白）
- **资源**：objectList → b/c/cg/sd；模糊/震动/闪光 → blur/e/f 标记
- **选项**：直接型 c1t=target；flag 型**先解析出真实目标**（见 5.1）

### 5.1 flag 型选项目标解析（重点）

flag 型选项**不要**直接解析到分支内容——因为 dummy 场景往往还有**公共内容**（如高潮铺垫），直接跳过会丢内容。正确做法：
1. 选项页 c1t 指向 **dummy 场景起点**（公共内容）
2. dummy 场景**末尾**加 hiddenPages 路由：`if(choice[选项页] === N) return 分支N; return 默认分支;`
3. 若 dummy 无内容（纯路由），c1t 可直接指向分支内容

### 5.2 多立绘缩减

手环一屏只能显示一个立绘。多立绘（`c` 字段 `;` 分隔多个）时按说话人保留：
1. 说话人匹配到场立绘 → 保留说话人那个
2. 无匹配（旁白/主角）→ 延续上一页立绘（若仍在场）
3. 兜底第一个

（说话人中文名 → 立绘日文名需建映射，如 艾莉娜→エリナ）

---

## 6. 合并与全局页表

70 个剧本 → **全局连续页码**，`scriptDataN.txt`（500 页/块）：

- **文件顺序**：主线在前、路线在后（可 Windows 自然排序）
- **label → 全局页**：每个 label 记录其在合并流的绝对页码（`.map.json`）
- **背景映射**：`stagename_timename` → 实际背景图（`_背景映射.json`）
- **CG 去重映射**：`cg_dedup_map.json`（`ev105ab → ev105aa`）

---

## 7. 分支配置生成（线性模型的 4 个机制）

| 机制 | branchConfig 字段 | 作用 | 生成规则 |
|---|---|---|---|
| 选项跳转 | 选项页 `c1t..c5t` | 玩家选择 → 跳分支 | 直接解析/flag 解析 |
| 前进特判 | `noNextPages` | {分支末页: 合流页}，跳过兄弟分支 | 场景默认 nexts 目标 ≠ 物理下一页时 |
| 回退堵死 | `noBackPages` | 合流自环 + 选项/路由回退 | 见 7.1 |
| 条件路由 | `hiddenPages` | flag 判定 → 路由页 | 见 7.2 |
| 结局 | `end` | {结局页: 文本} | 场景 nexts → start.ks/gameend |

### 7.1 noBackPages 规则（千恋万花同款）
1. **合流点自环**：`noBackPages[合流页] = 合流页`（堵死回退，防反悔）
2. **选项回退**：`noBackPages[选项跳转目标] = 选项页`（防返回进入兄弟分支内容）
3. **路由回退**：`noBackPages[路由目标] = 路由源`（回到跳出处）
4. **结局阻断**：`noBackPages[结局页] = 结局页`（后日谈结束防回退）

### 7.2 hiddenPages 条件推导

每个条件路由 = 一个 `(choice) => {...}` 函数，读 `choice[选项页]`（已选记录）：

- **H 场景路由**（sel_flag 由紧邻选项设置）→ 直接 `choice[选项页] === N`
- **跨场景 sel_flag**（如本编按此前选择切变体）→ 找**最近设置该 flag 的选项页**
- **路线分叉**（女孩 flag == 4）→ 用 flag 规则表推导"选满哪几个选项 → 满 4"

> 推导依据记录在生成文档（`flag_rules.md` / `跳转分类表.md`），可逐条对照原作核对。

### 7.3 常见坑
- **flag 选项跳过公共内容**（见 5.1）——转换器解析要指向 dummy 起点
- **合流页被多个分支到达**——noBackPages 自环堵死即可，不需要完美回溯
- **后日谈/额外章节**（nexts → start.ks 的多个出口）——end 标记 + lct/more 页码直跳进入

---

## 8. 资源处理

| 资源 | 处理 | 输出 |
|---|---|---|
| 立绘 | `.tlg` → PNG → 分层合成（身体+表情坐标） | `cimg/` + `char_data.txt` |
| 背景 | `stagename_timename` → 实际图，压缩 | `bg/*.jpg` |
| 事件 CG | 旋转/裁切 → 336×480 jpg | `evig/*.jpg` |
| SD 小人 | 透明 PNG → 336×480（**转 P 模式压缩**） | `evig/*.png` |

- 立绘合成数据 `char_data.txt`：`{角色: {pose: {body: {img,left,top,w,h,scale}, bodies:{dress:...}, faces:{id:{...}}, face_map:{}}}}`
- **压缩目标**：evig 总大小 < 10MB（手环内存限制）。SD 用 P 模式（调色板）可省 80%

---

## 9. 播放器搭建（快应用）

- `gameEngine.js`：分块加载 `scriptDataN.txt`（500 页/块，稀疏数组 + 清理旧块）
- `detail.ux` 播放器：
  - 读 script → 渲染背景/立绘(实时合成)/CG/文字
  - 选项页显示选项，`recordChoice` 记录选择
  - 翻页检查：`end` → `hiddenPages` → `noNextPages` → `+1`
  - 回退检查：`noBackPages` → `-1`
  - 立绘实时合成：读 `char_data.txt`，身体图 + 表情图按坐标叠加
- `branchConfig` 注入 `detail.ux` 的 data 对象
- 其他页面：index（标题）/ lct（流程图/章节跳转）/ cgs（CG 鉴赏）/ more（后日谈）/ settings

---

## 10. 验证与调试

1. **全量跳转分类表**（`extract_jumps.py` → `跳转分类表.md`）：每个选项/路由/静态跳转的源页→目标页，逐条核对
2. **结构校验**：
   - 所有页面存在
   - 所有 b/c/cg/sd 引用指向存在的文件
   - 所有跳转目标页有效
   - **可达性 BFS**：从 page 1 出发，线性+跳转应覆盖全部页面（100%）
3. **常见问题排查**：
   - 卡在某页翻不过去 → hiddenPages 没兜底 return
   - 跳到错误分支 → 选项目标解析/flag 条件核对
   - 显示空白图 → 资源引用缺失（查 b/c/cg/sd 是否存在文件）
   - 路线进不去 → 女孩 flag 累计条件写错

---

## FAQ

**Q: 一定需要 FreeMote 吗？**
反编译 .ks.scn 需要。也可用 KrkrExtract 等，但输出结构不同，转换器要适配。

**Q: 新作品没有 dummy 场景/flag 结构怎么办？**
KiriKiri2 柚子社系基本都有。其他引擎（吉里吉里/AIR）结构不同，需另写解析器，但"线性页表 + branchConfig"模型通用。

**Q: 立绘怎么合成？**
原作是 身体.tlg + 表情差分.tlg + 规则.txt。用 AutoCGAligner / krkrFgiEditor 按规则合成基础立绘，再叠加表情差分。本项目用 `build_char_data.py` 生成坐标数据，播放器实时拼装。

**Q: 手环内存不够怎么办？**
- scriptData 分块加载（500页/块）+ 旧块清理
- 图片做缓存 + 按优先级回收
- evig 压缩到 <10MB，SD 用 P 模式

**Q: 版权问题？**
仓库只放工具，不放游戏资源。README 已声明需正版游戏自备资源。
